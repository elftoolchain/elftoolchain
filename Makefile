# $Id$

SUBDIR +=
SUBDIR += ar
SUBDIR += brandelf
SUBDIR += elfcopy
SUBDIR += elfdump
SUBDIR += libelf
SUBDIR += libelf-test-suite
SUBDIR += libelf-tutorial
SUBDIR += libelftc
SUBDIR += nm
SUBDIR += readelf
SUBDIR += size
SUBDIR += strings

.include <bsd.lib.mk>
